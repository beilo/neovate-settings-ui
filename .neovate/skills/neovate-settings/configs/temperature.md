# temperature

**类型:** `number`  
**默认值:** `null`（使用模型默认值）

## 说明

调整模型响应的随机性。

- 较低值（如 0.1）：更确定性、更一致的输出
- 较高值（如 0.9）：更随机、更有创意的输出

范围通常为 0 到 1 或 0 到 2，取决于模型。

## 使用示例

```json
{
  "temperature": 0.3
}
```

## 相关配置

- model: 主模型设置
