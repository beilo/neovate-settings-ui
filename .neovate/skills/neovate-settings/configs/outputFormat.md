# outputFormat

**类型:** `"text" | "stream-json" | "json"`  
**默认值:** `"text"`

## 说明

选择 CLI 响应的输出格式。

- `text`: 纯文本格式，适合人类阅读
- `json`: JSON 格式，适合程序解析
- `stream-json`: 流式 JSON，适合实时处理

## 使用示例

```json
{
  "outputFormat": "json"
}
```

## 相关配置

- outputStyle: 输出样式
- quiet: 静默模式
